# Use DecapBridge classic auth (required)
backend:
  name: git-gateway
  repo: otakmemberontakart-lab/kinekon_preview_sistem
  branch: main
  identity_url: https://auth.decapbridge.com/sites/393a248b-22f6-4770-9c8a-6672879c6b23
  gateway_url: https://gateway.decapbridge.com
  # See who did what (optional)
  commit_messages:
    create: Create {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

# Better Decap + Bridge logo (optional)
logo_url: https://decapbridge.com/decapcms-with-bridge.svg
# Add site links in Decap CMS (optional)
site_url: https://kinekon-preview.netlify.app

media_folder: "images/uploads"
public_folder: "images/uploads"

collections:
  - name: "productions"
    label: "Produksi Terbaru"
    files:
      - name: "productions"
        label: "Galeri Produksi Terbaru"
        file: "content/productions.json"
        fields:
          - label: "Daftar Produksi"
            name: "items"
            widget: "list"
            summary: "{{title}}"
            fields:
              - {label: "Gambar", name: "image", widget: "image"}
              - {label: "Judul (opsional)", name: "title", widget: "string", required: false}
